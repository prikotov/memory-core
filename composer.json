{
    "name": "prikotov/memory-core",
    "description": "Key-value storage library with filesystem and other backends",
    "type": "library",
    "license": "MIT",
    "keywords": ["storage", "key-value", "filesystem", "memory", "cache"],
    "authors": [
        {
            "name": "Dmitry Prikotov",
            "email": "prikotov@gmail.com"
        }
    ],
    "minimum-stability": "stable",
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "Memory\\Core\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Memory\\Core\\Tests\\": "tests/"
        }
    },
    "require": {
        "php": "^8.4",
        "ext-json": "*",
        "symfony/console": "^8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "squizlabs/php_codesniffer": "^3.7",
        "phpstan/phpstan": "^1.10",
        "vimeo/psalm": "^5.0|^6.0"
    },
    "bin": [
        "bin/memory"
    ],
    "config": {
        "sort-packages": true,
        "platform-check": true
    },
    "scripts": {
        "test": "phpunit",
        "cs-check": "phpcs --standard=devops/phpcs/MemoryCodingStandard/ruleset.xml src tests",
        "cs-fix": "phpcbf --standard=devops/phpcs/MemoryCodingStandard/ruleset.xml src tests",
        "stan": "phpstan analyse",
        "psalm": "psalm",
        "post-install-cmd": [
            "@php vendor/bin/phpcs --config-set installed_paths $(pwd)/devops/phpcs"
        ],
        "post-update-cmd": [
            "@php vendor/bin/phpcs --config-set installed_paths $(pwd)/devops/phpcs"
        ]
    },
    "extra": {
        "branch-alias": {
            "dev-main": "1.0-dev"
        }
    }
}
